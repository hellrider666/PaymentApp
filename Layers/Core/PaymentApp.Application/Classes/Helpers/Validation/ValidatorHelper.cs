using FluentValidation;
using MediatR;
using PaymentApp.Application.Classes.Repositories;

namespace PaymentApp.Application.Classes.Helpers.Validation
{
    public static class ValidatorHelper
    {
        //public static async Task<TResult> ExistHelper<TResult, TRequest>(this AbstractValidator<TRequest> validator, Func<IUnitOfWork, Task<TResult>> action)
        //    where TRequest : IRequest
        //{
        //    using (var unitOfWork = action.get)
        //}
    }
}
